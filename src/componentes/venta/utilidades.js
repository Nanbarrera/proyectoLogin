// utilidades.js

// Función para eliminar un producto por su ID
export const handleDelete = (productId) => {
    // Lógica para eliminar el producto con productId
    console.log(`Eliminar producto con ID: ${productId}`);
};
